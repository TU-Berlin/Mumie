\emph{Test}:

\begin{itemize}
\item Der Dialog erscheint wie erwartet.
\item Das Dialogfenster erscheint auch, wenn man nur "file" statt
  "file.mata.mxl" im Dateiauswahlfenster angibt.
\item Wird die Option "Nein" gewählt, wird der Graph nicht gespeichert und der
  gespeicherte Graph unbelassen.
\item Wird die Option "Ja" gewählt, wird der Graph gespeichert und überschreibt
  den anderen Graph.
\item Wird die Option "Speichern unter" gewählt, wird der vorhandene Graph
  nicht verändert und es öffnet sich ein neues Dateiauswahlfenster.
\end{itemize}