\emph{Howto}:

Der Eintrag für den Dateinamen wird gesetzt, sobald ein Graph gespeichert oder
geladen wird, also ist das Laden und Speichern auszutesten und zu prüfen, ob
der erwartete Dateiname angezeigt wird.