\emph{Howto}:

Eine Kante wird markiert, so dass die Stützstellen der Kante
angezeigt werden. Dann wird die Kante zwischen zwei Stützstellen "angefasst"
und per Drag'n'Drop an eine beliebige Stelle gezogen.