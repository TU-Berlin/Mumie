\emph{Test}:

\begin{itemize}
\item Der Dialog erscheint wie erwartet.
\item Das Dialogfenster erscheint auch, wenn man nur "file" statt
  "file.mata.mxl" im Dateiauswahlfenster angibt.
\item Wird die Option "Speichern wiederholen" gewählt, wird nur ein neues
  Dateiauswahlfenster geöffnet.
\item Wird die Option "Abbrechen" gewählt, wird der Graph nicht gespeichert und der
  andere Graph nicht verändert.
\item Wird die Option "Speichern und Dateinamen des anderen Kurses verwerfen"
  gewählt, so wird der Graph gespeichert und der Dateiname des anderen Graphen
  wird gelöscht.
\end{itemize}